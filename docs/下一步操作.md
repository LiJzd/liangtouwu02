# 下一步操作指南

## 当前状态 ✓

备份已完成！Git commit 已创建（commit: `03f5494`）

虽然标签创建时出现了错误（因为 wmic 命令不可用），但这不影响备份的有效性。commit 本身就是最好的备份点。

## 立即执行

### 方案 1：直接运行重组（推荐）

```bash
scripts\reorganize_project.bat
```

如果出现问题，可以回滚到备份点：
```bash
git reset --hard 03f5494
```

### 方案 2：先创建标签，再重组

如果你想要一个命名标签方便记忆：

```bash
# 1. 创建标签
scripts\create_backup_tag.bat

# 2. 运行重组
scripts\reorganize_project.bat
```

回滚命令：
```bash
git reset --hard backup-reorganize
```

## 重组后的验证步骤

1. 检查目录结构是否正确
2. 测试后端服务：`cd backend && mvn spring-boot:run`
3. 测试前端服务：`cd frontend && npm run dev`
4. 测试AI服务：`cd ai-service && python main.py`
5. 或使用统一启动：`scripts\start-all.bat`

## 可能需要更新的配置

重组后，以下配置文件可能需要更新路径：

- `ai-service/.env` - 检查模型路径、视频路径
- `backend/application.yml` - 检查资源路径
- `frontend/vite.config.js` - 检查静态资源路径

## 回滚方法

如果重组后发现问题，可以安全回滚：

```bash
# 回滚到重组前
git reset --hard 03f5494

# 清理未跟踪的文件（可选）
git clean -fd
```

## 文件清理

重组脚本会自动删除以下临时文件：
- `-p/` 空目录
- `pip-cache/` Python缓存
- `pip-tmp/` Python临时文件
- `logs/` 日志文件
- `user_check.txt` 临时文件

## 新的项目结构

```
liangtouwu-project/
├── backend/          # 两头乌后端 (Spring Boot)
├── frontend/         # 两头乌前端 (Vue 3)
├── ai-service/       # 两头乌ai端 (FastAPI + ReAct Agent)
├── resources/        # 共享资源（模型、视频、PPT）
├── database/         # 数据库脚本和文档
├── scripts/          # 启动和管理脚本
└── docs/             # 项目文档
```

---

**准备好了吗？运行 `scripts\reorganize_project.bat` 开始重组！** 🚀
